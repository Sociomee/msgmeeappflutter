import 'package:flutter/material.dart';

class WifiNetworkTab extends StatelessWidget {
  const WifiNetworkTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('WIFI')),
    );
  }
}
