import 'package:flutter/material.dart';

class RoamingNetworkTab extends StatelessWidget {
  const RoamingNetworkTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Roaming')),
    );
  }
}
