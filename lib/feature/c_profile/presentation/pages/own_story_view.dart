import 'package:flutter/material.dart';

class OwnStoryView extends StatelessWidget {
  const OwnStoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          
        ],
      ),
    );
  }
}
