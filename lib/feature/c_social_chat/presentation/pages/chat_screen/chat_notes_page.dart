import 'package:flutter/material.dart';

class ChatNotesPage extends StatelessWidget {
  const ChatNotesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}